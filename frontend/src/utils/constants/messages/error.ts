const TRY_AGAIN_LATER = "Tente novamente mais tarde"
const PERSIST_PROBLEM = "Se o problema persistir, contate o administrador do sistema."

export const ERROR_MESSAGES = {
    TRY_AGAIN_LATER,
    PERSIST_PROBLEM
}